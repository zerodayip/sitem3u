name: Keep Proxy Alive

on:
  schedule:
    - cron: '10 */5 * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: 5 saat boyunca her 10 dakikada bir ping at
        run: |
          END_TIME=$((SECONDS + 5 * 60 * 60))  # 5 saat = 5 * 60 * 60 saniye

          while [ $SECONDS -lt $END_TIME ]; do
            echo "🔄 Yeni ping atılıyor: $(date '+%Y-%m-%d %H:%M:%S')"
            HTML=$(curl -s https://diziboxproxy.onrender.com/proxy/ping)
            TEXT=$(echo "$HTML" | grep -oP '(?<=<h1>).*?(?=</h1>)')
            
            if [[ -z "$TEXT" ]]; then
              echo "❌ Sistem pasif olabilir!"
            else
              echo "✅ $TEXT"
            fi
            
            echo "⏳ 10 dakika bekleniyor..."
            sleep 600  # 10 dakika
          done

          echo "✅ 5 saatlik çalışma tamamlandı."
